import { SourceProvider } from '../../SourceProvider';
import { NowPlayingTrack } from '../../types';
export interface LastFMSourceProviderOptions {
    apiKey: string;
    user: string;
}
export declare class LastFMSourceProvider extends SourceProvider<LastFMSourceProviderOptions> {
    protected getNowPlaying(): Promise<NowPlayingTrack | null>;
    private get recentTracksUrl();
}
