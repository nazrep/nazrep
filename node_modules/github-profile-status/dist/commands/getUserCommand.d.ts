import { UserStatus } from '../types';
import { BaseCommand } from './baseCommand';
interface Payload {
    user: {
        status: UserStatus | null;
    };
}
export declare class GetUserCommand extends BaseCommand<Payload['user']['status']> {
    perform(user: string): Promise<UserStatus | null>;
}
export {};
