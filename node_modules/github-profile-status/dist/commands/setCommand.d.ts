import { ChangeUserStatusInput, UserStatus } from '../types';
import { BaseCommand } from './baseCommand';
interface Payload {
    changeUserStatus: {
        status: UserStatus | null;
    };
}
export declare class SetCommand extends BaseCommand<Payload['changeUserStatus']['status']> {
    perform(status: ChangeUserStatusInput): Promise<UserStatus | null>;
}
export {};
