import { UserStatus } from '../types';
import { BaseCommand } from './baseCommand';
interface Payload {
    viewer: {
        status: UserStatus | null;
    };
}
export declare class GetViewerCommand extends BaseCommand<Payload['viewer']['status']> {
    perform(): Promise<UserStatus | null>;
}
export {};
