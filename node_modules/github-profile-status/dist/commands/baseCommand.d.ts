import { GraphQLClient } from '../graphQLClient';
export declare abstract class BaseCommand<Payload> {
    protected client: GraphQLClient;
    constructor(client: GraphQLClient);
    perform(...args: any[]): Promise<Payload>;
}
